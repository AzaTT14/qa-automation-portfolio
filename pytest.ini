[pytest]
markers =
    smoke: quick smoke-suite tests, run before every release
    regression: full regression suite
    negative: negative / boundary test cases
testpaths = tests
addopts = -ra --strict-markers
